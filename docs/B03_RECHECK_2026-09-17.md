# B03 当前实现复核（2026-09-17）

结论：B03 原审查 R1–R5 和剩余第 3、5、6 条已在提交 `b94a9b88372a7c513da66c455611c32cd89fd8f1` 中实现。本次亲自检查实际代码并重新运行下述验收，未发现该支持矩阵中的新增失败；无需重复实现已完成的功能。

## 本次范围与版本

- 当前分支：`codex/stage1-b04-b06`，HEAD 为上述 B03 提交。
- 源码检查使用当前工作区，包含尚未提交的 B04–B06 修改。本次结果是 B03 功能在该工作区的回归证明，不是对 B04–B06 全部任务的验收。
- UMD 用例使用当时磁盘中的 `build/0.1.0/CCR.min.js`，SHA-256 为 `a959bf6696e983d59ed848f260042a498aacfec1b4a55e30964265cc44bc5fe1`。该产物已有 B04 修改，清单中有 22 个已记录源文件与当前工作区不同；不能将这些 UMD 用例说成当前全部源码的构建验收。本次未重新打包或发布。
- 本次未修改运行代码，未覆盖既有 B04–B06 改动，未新建提交或推送。B03 原完整交付记录仍见 [B03_COMPLETION.md](B03_COMPLETION.md) 和 [B03_VALIDATION.json](B03_VALIDATION.json)。

## 原剩余任务逐项核验

| 原任务 | 实现与本次证据 |
| --- | --- |
| 第 3 条：SSR 替换环境镜面 | `DeferredLighting143` 同次照明输出镜面及响应，复用既有 SSR tracer；在 OIT 前以带置信度的差量替换，未命中保留环境镜面。28 项数值检查通过；314,556 个有效像素与原生环境镜面对照，无超门槛分量；20,536 个命中，60,968 个不透明 RGB 通道有实际变化；强度 0 与关闭差异为 0。玻璃 delta、合成及最终显示均有实际贡献。 |
| 第 5 条：水与粒子透明前向 | 真实 Water、ParticleSystem 在 OIT/排序两种模式均通过。水面接收 CCR 阴影，HDR 最大贡献差约 0.139；粒子按自发光语义处理，透明边缘和 alpha=1 核心均响应开关，恢复误差小于 0.001。未将粒子冒充为带表面法线的 PBR 模型。 |
| 第 6 条：MASK/异步 tile/style/轮廓 | 两模式 MASK 孔洞和实体对照误差 0；迟到 tile 实际加载、受补丁并可拾取；style 对照和恢复误差 0；实际 OPAQUE↔TRANSLUCENT 切换通过；轮廓产生 1,238 个像素，拾取身份保持，移除轮廓后恢复。复杂模板轮廓显式走增强兼容。 |

R1–R5 的代码核对包括：保留原始 shader；uniform 读取当前阴影和分项开关；停用/回退恢复命令；销毁统一卸载桥与资源；逐命令分发遵循停用、销毁及回调内卸载门禁。相关单元测试与真实透明混合检查均通过。

## 本次实际运行

| 检查入口 | 结果 |
| --- | --- |
| `npm test` | 470/470，0 失败 |
| `check-deferred-transparency.cjs` | MRT、multipass、排序、现有 UMD、无全局 Cesium 五种配置通过。单层误差分别约 0.002197、0.000244、0.000122、0.002197、0.002197，固定门槛 0.01。 |
| `check-deferred-transparency.cjs --disable-forward` | 预期退出 1，明确由 `transparent lighting negative-control gate` 拒绝；证明测试不能在 CCR 模块缺失时假通过。 |
| `check-deferred-ssr.cjs` | 上述数值、环境镜面对照和实际输出检查通过；resize 释放旧附件，切回 enhanced 后额外附件字节归零。 |
| `check-transparent-families.cjs` | 两模式真实水、粒子与两种销毁顺序通过。 |
| `check-transparency-content.cjs` | 两模式动态内容、兼容 Primitive、生成影像、classification 对照与自动恢复通过。 |
| `check-deferred-recovery.cjs` | 7 组故障/能力回退均恢复当帧颜色并可重试；AO 消费者恢复通过。 |
| `check-deferred-matrix.cjs` | 22 组通过，含多视锥、南半球/极地、MSAA/排序兼容、resize、viewer 先销毁、context loss 和绘制 override。 |
| `check-frame-bridge.cjs` | 7 种配置通过，含 MRT、MSAA 4x、multipass、10 个视锥、排序透明、无玻璃和现有 UMD；拾取、深度和资源恢复门槛通过。 |
| `git diff --check` | 无空白错误；Windows 行尾提示不影响结果。 |

浏览器检查采用本机 Chrome/WebGL2、通用生成模型和影像，不依赖校园模型或外部影像服务。各检查无页面/渲染错误。原始结果在本机 `docs/verification/stage1-B03-fixed/`，关联回归在 `stage1-B02-recovery/`、`stage1-B02-matrix/`、`stage1-B01-fixed/`；单测日志为 `stage1-B03-fixed/recheck-unit.log`。

## 完成边界

OIT 加权叠层相对于精确排序的约 0.068–0.069 误差仍是已公开的算法近似，不作为精确排序达标。MSAA 下延迟几何、排序模式的不透明延迟接点、TAA 延迟组合和云雾视线分段仍不在 B03 完成结论内。任意自定义材质及复杂模板采用公开兼容路径；本次也没有做正式 FPS/P95 或全部真实资产认证。
